require 'spec_helper'

describe List do

  # it "should add a list" do
  #   user = Factory.create(:user)
  #   list = List.new
  #   ingredients = []
  #   ingred = Factory.attributes_for(:ingredient)
  #   ingred.each do |i| 
  #     ingredients << ingred[:name]
  #   end
  #   list.add(user.id, ingredients)
  #   list.get(user.id).should_not be_nil
  # end

  # it "should delete a list" do
  #   user = Factory.create(:user)
  #   list = Factory.build(:list)
  #   list.add(user_id, list)
  #   list.delete(user_id)
  #   list.should be_valid
  # end

  # it "should get a list" do
  #   user = Factory.create(:user)
  #   list = Factory.build(:list)
  #   list.get(user_id)
  #   list.should be_valid
  # end

end
